name: shopsphere
version: "1.0.0"
config-version: 2

profile: shopsphere

model-paths: ["models"]
snapshot-paths: ["snapshots"]

models:
  shopsphere:
    staging:
      +materialized: view

    marts:
      +materialized: table
      